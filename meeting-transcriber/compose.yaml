services:
  transcriber:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file: backend/.env
    volumes:
      - ./data:/app/data
    expose:
      - "8000"
    ports:
      - "8001:8000"
    restart: unless-stopped
