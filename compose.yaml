services:
  transcriber:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file: .env
    environment:
      - PORT=${PORT:-8000}
    volumes:
      - ./data:/app/data
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    restart: unless-stopped
